import React from 'react';

type Props = {
};
type State = {
};

export class Filters extends React.PureComponent<Props, State> {
  render() {
    return (
      <div>
        <h3>Filters</h3>
        <h5>there are filter exaples <a href="https://developer.mozilla.org/en-US/docs/Web/SVG/Element/filter">https://developer.mozilla.org/en-US/docs/Web/SVG/Element/filter</a></h5>
        <h5>this chapter not done now</h5>
      </div>

    );
  }
}
